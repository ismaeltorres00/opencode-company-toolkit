# Scope model

- global: every project
- technology: opt-in by stack, e.g. dotnet/node/frontend
- domain: opt-in by business/domain
- project: lives in the consumer repository

A project should register only the catalogs it needs.
