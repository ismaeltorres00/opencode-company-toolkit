# MCP

Mantener aqui ejemplos/documentacion, nunca secretos.
Los servidores MCP se habilitan desde `opencode.json(c)` por proyecto o globalmente.
Las credenciales deben venir de variables de entorno u OAuth.
